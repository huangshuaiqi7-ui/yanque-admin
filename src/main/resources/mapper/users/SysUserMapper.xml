<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yanque.modules.users.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="cn.yanque.modules.users.pojo.entity.SysUserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="unionId" column="union_id"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, username, password, nickname, real_name, phone, email, union_id, status, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="SysUserMap">
        select <include refid="BaseColumns"/> from sys_user where id = #{id}
    </select>

    <select id="selectByUsername" resultMap="SysUserMap">
        select <include refid="BaseColumns"/> from sys_user where username = #{username}
    </select>

    <select id="selectPage" resultMap="SysUserMap">
        select <include refid="BaseColumns"/> from sys_user u
        <where>
            <if test="keyword != null and keyword != ''">
                and (u.username like concat('%', #{keyword}, '%')
                or u.nickname like concat('%', #{keyword}, '%')
                or u.real_name like concat('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                and u.status = #{status}
            </if>
            <if test="roleCode != null and roleCode != ''">
                and exists (select 1 from sys_user_role ur
                inner join sys_role r on r.id = ur.role_id
                where ur.user_id = u.id and r.role_code = #{roleCode})
            </if>
        </where>
        order by u.id desc
    </select>

    <select id="selectRoleIds" resultType="java.lang.Long">
        select role_id from sys_user_role where user_id = #{userId} order by role_id
    </select>

    <delete id="deleteUserRoles">delete from sys_user_role where user_id = #{userId}</delete>

    <insert id="insertUserRoles">
        insert into sys_user_role(user_id, role_id, created_at) values
        <foreach collection="roleIds" item="roleId" separator=",">(#{userId}, #{roleId}, now())</foreach>
    </insert>

    <insert id="insert" parameterType="cn.yanque.modules.users.pojo.entity.SysUserEntity"
            useGeneratedKeys="true" keyProperty="id">
        insert into sys_user
        (username, password, nickname, real_name, phone, email, union_id, status, created_at, updated_at)
        values
        (#{username}, #{password}, #{nickname}, #{realName}, #{phone}, #{email}, #{unionId},
         #{status}, now(), now())
    </insert>

    <update id="updateById" parameterType="cn.yanque.modules.users.pojo.entity.SysUserEntity">
        update sys_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="unionId != null">union_id = #{unionId},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = now()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from sys_user where id = #{id}
    </delete>
</mapper>
