<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yanque.modules.aichat.mapper.AiChatMapper">
    <!-- 左侧会话列表：只查当前学生未删除的会话，按最近更新时间倒序。 -->
    <select id="selectSessions" resultType="cn.yanque.modules.aichat.pojo.entity.AiChatSessionEntity">
        select id, title, updated_at
        from ai_chat_session
        where student_id = #{studentId} and status = 'ACTIVE'
        order by updated_at desc, id desc
    </select>

    <!-- 会话详情：同时带 student_id 条件，避免学生访问别人的会话。 -->
    <select id="selectSession" resultType="cn.yanque.modules.aichat.pojo.entity.AiChatSessionEntity">
        select id, student_id, title, status, summary, last_compressed_message_id, created_at, updated_at
        from ai_chat_session
        where id = #{id} and student_id = #{studentId} and status = 'ACTIVE'
    </select>

    <!-- 后台压缩使用：send 入口已经校验过会话归属，这里只按会话 ID 读取摘要字段。 -->
    <select id="selectSessionById" resultType="cn.yanque.modules.aichat.pojo.entity.AiChatSessionEntity">
        select id, student_id, title, status, summary, last_compressed_message_id, created_at, updated_at
        from ai_chat_session
        where id = #{id} and status = 'ACTIVE'
    </select>

    <!-- 历史消息展示：打开会话时按创建顺序返回。 -->
    <select id="selectMessages" resultType="cn.yanque.modules.aichat.pojo.entity.AiChatMessageEntity">
        select id, session_id, role, content, model, tokens, created_at
        from ai_chat_message
        where session_id = #{sessionId}
        order by created_at asc, id asc
    </select>

    <!-- AI 上下文：MVP 先取未压缩消息，后续可以在这里加条数或 token 限制。 -->
    <select id="selectContextMessages" resultType="cn.yanque.modules.aichat.pojo.entity.AiChatMessageEntity">
        select id, session_id, role, content, model, tokens, compressed, created_at
        from ai_chat_message
        where session_id = #{sessionId} and compressed = 0
        order by created_at asc, id asc
    </select>

    <!-- 压缩判断：取当前所有未压缩消息，超过阈值时把较早的消息压进 summary。 -->
    <select id="selectUncompressedMessages" resultType="cn.yanque.modules.aichat.pojo.entity.AiChatMessageEntity">
        select id, session_id, role, content, model, tokens, compressed, created_at
        from ai_chat_message
        where session_id = #{sessionId} and compressed = 0
        order by created_at asc, id asc
    </select>

    <!-- 新建会话：status 固定 ACTIVE，summary 和压缩位置先用默认值。 -->
    <insert id="insertSession" useGeneratedKeys="true" keyProperty="id">
        insert into ai_chat_session(student_id, title, status, summary, last_compressed_message_id)
        values(#{studentId}, #{title}, 'ACTIVE', #{summary}, 0)
    </insert>

    <!-- 保存消息：user 消息没有 model/tokens，assistant 消息会带模型和用量。 -->
    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="id">
        insert into ai_chat_message(session_id, role, content, model, tokens, compressed)
        values(#{sessionId}, #{role}, #{content}, #{model}, #{tokens}, #{compressed})
    </insert>

    <!-- 写摘要和水位线：summary 负责“压成什么”，last_compressed_message_id 负责“压到哪”。 -->
    <update id="updateSummary">
        update ai_chat_session
        set summary = #{summary}, last_compressed_message_id = #{lastCompressedMessageId}, updated_at = now()
        where id = #{id}
    </update>

    <!-- 批量打标：水位线之前的未压缩消息都已经进入 summary。 -->
    <update id="markCompressed">
        update ai_chat_message
        set compressed = 1
        where session_id = #{sessionId}
          and compressed = 0
          and id &lt;= #{lastCompressedMessageId}
    </update>

    <!-- 有新消息后刷新会话时间，保证左侧列表排序正确。 -->
    <update id="touchSession">
        update ai_chat_session set updated_at = now() where id = #{id}
    </update>

    <!-- 删除会话采用逻辑删除，历史消息保留，方便后续审计或恢复。 -->
    <update id="deleteSession">
        update ai_chat_session
        set status = 'DELETED', updated_at = now()
        where id = #{id} and student_id = #{studentId} and status = 'ACTIVE'
    </update>
</mapper>
